<UserControl x:Class="LiveProcessInspector.AvaliableProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LiveProcessInspector"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Width="300" Height="300" Background="LightBlue">
        <ListView x:Name="Processes"
                  SelectedItem="{Binding SelectedProcess}"
                  cal:Message.Attach="[Event MouseDoubleClick] = [Action AttachToProcess()]"                 
                  Margin="20" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center">
            
        </ListView>
    </Grid>
</UserControl>
